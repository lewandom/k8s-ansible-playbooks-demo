---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ mysql_name }}-lv-claim"
  labels:
    app: "{{ mysql_name }}"
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  selector:
    matchLabels:
      app: "{{ mysql_name }}"
